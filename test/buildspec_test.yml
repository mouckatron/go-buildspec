version: 0.2
env: []
phases:
  install:
    environment:
      NAME: buildspector
    commands:
      - echo Hello $NAME
  pre_build:
    commands: []
  build:
    commands: []
  post_build:
    commands: []
